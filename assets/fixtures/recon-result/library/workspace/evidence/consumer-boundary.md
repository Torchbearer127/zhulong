# Consumer boundary evidence

The parser API is called by a consuming application.  A later verifier must
construct that consumer boundary separately; this Recon contract does not
create a candidate or claim an impact.
