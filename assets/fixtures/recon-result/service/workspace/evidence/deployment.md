# Deployment evidence

The target contract declares a Docker Compose application service and a local
verification network.  These are default deployment assumptions for the
fixture contract, not runtime execution evidence.
