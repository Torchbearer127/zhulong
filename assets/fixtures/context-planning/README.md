# Context Planning Fixtures

These tiny local repositories exercise the production context catalog, planner,
and plan validator. They contain no credentials, targets, Docker files, PoCs,
or network configuration. The fixture manifest records the expected advisory
selection cases; negative documents are synthesized by the selftest so they do
not become a second catalog source of truth.
